<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shelter_project.infoBoard.CommentMapper">
		
	<insert id="addComment">
		insert into comments values(comments_seq.nextval,#{postNo},#{content},#{author},
		#{createdDate},#{createdDate},#{parentNo},#{orderNumber})
	</insert>
	
	<select id="getComments">
		select * from comments where postNo = #{postNo} ORDER BY orderNumber ASC, commentNo ASC
	</select>
	<select id="getCount">
		select count(*) from comments where postNo = #{postNo}
	</select>
	<select id="getMaxOrderNumber">
		select NVL(MAX(orderNumber),0)
		from comments
		where postNo = #{postNo}
	</select>
	<select id="getOrderNumber">
		select orderNumber
		from comments
		where commentNo = #{commentNo}
	</select>
</mapper>